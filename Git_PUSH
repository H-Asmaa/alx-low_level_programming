#!/bin/bash

# Prompt the user to enter a commit message
read -p "Enter commit message: " message

# Stage all changes
git add .

# Commit changes with the provided message
git commit -m "$message"

# Push changes to the origin remote on the current branch
git push
